# Local development (gateway on port 5000)
VITE_API_URL=http://localhost:5000

# Optional: override asset base path (dev default is /)
# VITE_BASE_PATH=/

# --- GitHub Pages (production build) ---
# Copy .env.production.example → .env.production for local production builds.
# Or rely on GitHub Actions (see .github/workflows/deploy-pages.yml).
